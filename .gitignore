# generated by the edit loop
_blk0.js
*.bak
_mutants/
__pycache__/

# the relay's store: hosted lists, documents, and the token it made for itself
interop/_relay.json
interop/_reports.jsonl

# dependencies of the verification harnesses (npm ci restores them)
interop/node_modules/

# the author's own close-out script: copies to a Downloads folder and names private pages
interop/deliver.py
